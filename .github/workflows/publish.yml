name: Publish to PyPI

# OIDC-only (ADR-278): kein Token, Trusted Publisher auf PyPI = (achimdehnert/iil-enrichment, publish.yml, environment pypi).
# Closes #2 — unblocks risk-hub consumption of iil-enrichment (issue #177 there).
# Build-Backend: hatchling (dieses Paket).
# Gates vor dem irreversiblen Upload: Tests, Tag == pyproject.version, gitleaks auf dem gebauten Artefakt (ADR-226).
#
# PREREQUISITE (einmalig, PyPI-seitig): Trusted Publisher fuer dieses Repo registrieren
# unter https://pypi.org/manage/account/publishing/ —
#   PyPI project: iil-enrichment · Owner+Repo: achimdehnert/iil-enrichment ·
#   Workflow: publish.yml · Environment: pypi
# Wird das Repo in die iilgmbh-Org transferiert, muss das Binding auf
# iilgmbh/iil-enrichment umgezogen werden (Binding ist org/repo-spezifisch, der Workflow nicht).

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      target:
        description: "Upload target"
        required: true
        default: "pypi"
        type: choice
        options:
          - pypi
          - testpypi

jobs:
  test:
    name: Tests (publish gate)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install package + dev deps
        run: |
          pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Run tests (wie CI)
        run: pytest tests/ --tb=short -q

  build:
    name: Build distribution
    runs-on: ubuntu-latest
    needs: test
    outputs:
      version: ${{ steps.meta.outputs.version }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install build tools
        run: pip install hatchling twine
      - name: Read version from pyproject.toml
        id: meta
        run: |
          VERSION=$(python3 -c "import tomllib; print(tomllib.load(open('pyproject.toml','rb'))['project']['version'])")
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Building version: $VERSION"
      - name: Tag must equal pyproject version (K3, ADR-266)
        if: github.event_name == 'push'
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          if [ "$TAG" != "${{ steps.meta.outputs.version }}" ]; then
            echo "::error::Tag v$TAG != pyproject version ${{ steps.meta.outputs.version }} — kein Upload"
            exit 1
          fi
      - name: Build wheel + sdist
        run: |
          rm -rf dist
          python -m hatchling build
          ls -la dist/
      - name: Check
        run: twine check dist/*
      # ADR-226 binding gate: scannt exakt die Bytes, die hochgeladen werden.
      - name: Pre-publish secret gate (built artifact)
        uses: achimdehnert/platform/.github/actions/gitleaks-scan@main
        with:
          mode: sdist
          source: dist
          report_escape_hatch: "false"
          context_label: "pre-publish: iil-enrichment"
          triage_hint: "A secret is in the artifact about to be published to PyPI (irreversible). ROTATE it and exclude it from the build. NOT suppressible."
      - name: Upload dist artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist-${{ steps.meta.outputs.version }}
          path: dist/
          retention-days: 7

  publish-pypi:
    name: Publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    if: |
      (github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')) ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.target == 'pypi')
    environment:
      name: pypi
      url: https://pypi.org/project/iil-enrichment/
    permissions:
      id-token: write
    steps:
      - name: Download dist artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist-${{ needs.build.outputs.version }}
          path: dist/
      - name: Publish to PyPI (Trusted Publishing, PEP 740 attestations)
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          print-hash: true
          skip-existing: true

  publish-testpypi:
    name: Publish to TestPyPI
    needs: build
    runs-on: ubuntu-latest
    if: |
      github.event_name == 'workflow_dispatch' && github.event.inputs.target == 'testpypi'
    environment:
      name: testpypi
      url: https://test.pypi.org/project/iil-enrichment/
    permissions:
      id-token: write
    steps:
      - name: Download dist artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist-${{ needs.build.outputs.version }}
          path: dist/
      - name: Publish to TestPyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
          print-hash: true
